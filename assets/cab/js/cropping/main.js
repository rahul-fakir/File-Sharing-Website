$(function () {
  /*
  var croppImage = $('#cropperImgModal .croppImg').cropper({
        image : $('#cropperImgModal .croppImg'),
        dataX : $('#dataX'),
        dataY : $('#dataY'),
        dataHeight : $('#dataHeight'),
        dataWidth : $('#dataWidth'),
        options : {
          aspectRatio: 1 / 1,
          crop: function (data) {
            dataX.val(Math.round(data.x));
            dataY.val(Math.round(data.y));
            dataHeight.val(Math.round(data.height));
            dataWidth.val(Math.round(data.width));
            //$dataRotate.val(Math.round(data.rotate));
        }
      }
  });
  
  
  
  $('#cropperImgModal #inputImage').change(function() {
    //croppImage.replace($('#inputImage').val());
    $().cropper('replace', $('#inputImage').val());
    alert('edit');
  });
  
  
  function strartOpa(url)
  {
    $().cropper('replace', url);
    alert('edit');
  };
  */
  
});